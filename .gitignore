# Rust build artifacts
/target/
/opt_engine/target/

# Python / maturin build artifacts
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/
*.so
*.pyd
.venv/
venv/

# Maturin editable-install artifacts inside the Python package (renamed to hola_opt).
# The __init__.py and __init__.pyi are source; the compiled .so (already covered by
# the broad *.so rule above) and the dashboard/ bundle are generated by
# `maturin develop` or `maturin build`.
hola-py/hola_opt/dashboard/

# Checkpoint temp files
*.tmp

# MkDocs build output
/site/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# Benchmark results
hola-py/benchmark_results/

# Dev-only / private
CLAUDE.md
.claude/
paper/
/issues/

# OS
.DS_Store
Thumbs.db
